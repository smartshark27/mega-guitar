# Tab Converter

Python3 script that reads PDF files of tabs and converts them into a JSON file, which will be used in the main application.

The JSON file should contain data on the chords required, lyrics and when to play chords in line with lyrics.
